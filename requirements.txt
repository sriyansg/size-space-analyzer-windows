# --- Requirements ---

# 1. CustomTkinter (Modern Variant)
# If you are running `analyze_modern.py`, you will need to install CustomTkinter:
customtkinter>=5.2.2

# 2. Standard Library (Classic Variant)
# If you are running `analyze.py`, there are no external dependencies required!
# It relies solely on built-in libraries: os, json, tkinter, threading, subprocess, datetime
